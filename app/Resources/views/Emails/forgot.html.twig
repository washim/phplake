Hello {% if user.name is empty %}{{user.username}}{% else %}{{user.name}}{% endif %},
Reset your account by clicking following url {{url('resetpassword', {'powerkey':powerkey,'key':key})}}

Thanks and Regards,
Phplake Support Team
